# VisionVoiceAsist v3.0 — Python Asılılıqları
# Quraşdırma: pip install -r requirements.txt
# Raspberry Pi üçün: pip install -r requirements.txt --break-system-packages

# ── Kompüter Görməsi & AI ─────────────────────────────────────────────────────
ultralytics>=8.2.0          # YOLOv8 obyekt aşkarlama
opencv-python-headless>=4.9.0  # Kamera, görüntü emalı (başlıqsız Pi üçün)
numpy>=1.26.0               # Rəqəmsal matris əməliyyatları

# ── Süni İntellekt ────────────────────────────────────────────────────────────
google-generativeai          # Claude Vision API

# ── Nitq Sistemi (TTS) ────────────────────────────────────────────────────────
requests>=2.32.0            # ElevenLabs API çağrıları
# Sistem: sudo apt install mpg123 espeak espeak-data -y

# ── Mətn Tanıma (OCR) ─────────────────────────────────────────────────────────
pytesseract>=0.3.10         # Python-Tesseract körpüsü
# Sistem: sudo apt install tesseract-ocr tesseract-ocr-aze -y

# ── Sistem İzləmə ─────────────────────────────────────────────────────────────
psutil>=5.9.8               # Batareya, CPU, yaddaş

# ── Raspberry Pi GPIO (İsteğe bağlı) ─────────────────────────────────────────
RPi.GPIO>=0.7.0             # GPIO vibrasiya motoru
# Qeyd: Pi olmayan sistemlərdə bu paket quraşdırılmasa da işləyir
